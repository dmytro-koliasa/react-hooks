export enum ValidatorType {
  IS_REQUIRED = 'is-required',
  MAX_LENGTH = 'max-length',
  MIN_LENGTH = 'min-length',
  REQUIRED_WITH = 'required-with',
  REQUIRED_IF_EQUALS = 'required-if-equals',
  SAME_WITH = 'same-with',
  HAS_NUMBER = 'has-number',
  HAS_CAPITALIZE = 'has-capitalize',
  HAS_SPEC_SYMBOL = 'has-spec-symbol',
  REGEXP = 'regexp',
  ONE_OF = 'one-of',
}
